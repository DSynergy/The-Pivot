<div id="menu">
  <h1 class="center">Manage Items</h1><br>
  <%= link_to  'Create Item', new_admin_listing_path, class: 'btn btn-info btn-lg center' %><br><br>
  <div class="row">
    <% @listings.each do |listing| %>
      <div class="floaters">
        <h4 class="listing-title"> <%= link_to  listing.name, edit_admin_listing_path(listing) %> </h4>
      </div>
    <% end %>
  </div>
</div>
